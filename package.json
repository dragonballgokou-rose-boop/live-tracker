{
  "name": "live-tracker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:cap": "BUILD_TARGET=capacitor vite build",
    "cap:sync": "npm run build:cap && npx cap sync",
    "cap:ios": "npm run build:cap && npx cap sync && npx cap open ios"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
