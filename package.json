{
  "name": "live-tracker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "build:cap": "BUILD_TARGET=capacitor vite build",
    "cap:sync": "npm run build:cap && npx cap sync",
    "cap:ios": "npm run build:cap && npx cap sync && npx cap open ios",
    "test:news": "node scripts/test-news-tours.mjs"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^6.0.0",
    "web-push": "^3.6.7"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0"
  }
}
